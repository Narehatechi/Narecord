# Based partially on Alyxia's work, thanks so much!

name: Release
on:
  push:
    tags:
      - v*

env:
  FORCE_COLOR: true

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-24.04

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.2"

      - uses: actions/cache@v5
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Install Linux dependencies
        run: |
          sudo apt update
          sudo apt install -y pkg-config libsdl2-dev libx11-dev libxcursor-dev libxrandr-dev libxinerama-dev libxi-dev libglx-dev libgl1-mesa-dev libxxf86vm-dev libglfw3-dev libwayland-dev libwayland-egl1 libegl-dev libxkbcommon-dev wayland-protocols extra-cmake-modules

      - name: Install Go dependencies
        run: go get -v

      - name: Recreate vendor and apply Wayland patches
        run: |
          go mod vendor
          git apply patches/wayland.patch

      - name: Build GUI (X11)
        run:
          CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -v -tags static -ldflags "-s
          -w -X 'narelotl/buildinfo.InstallerGitHash=$(git rev-parse --short
          HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{ github.ref_name }}'" -o
          Narelotl-x11

      - name: Build GUI (Wayland)
        run:
          CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -v -tags "static wayland"
          -ldflags "-s -w -X 'narelotl/buildinfo.InstallerGitHash=$(git
          rev-parse --short HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{
          github.ref_name }}'" -o Narelotl-wayland

      - name: Build GUI (Combined)
        run:
          CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -v -tags "static combined"
          -ldflags "-s -w -X 'narelotl/buildinfo.InstallerGitHash=$(git
          rev-parse --short HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{
          github.ref_name }}'" -o Narelotl

      - name: Build CLI
        run:
          CGO_ENABLED=1 GOOS=linux GOARCH=amd64 go build -v -tags "cli" -ldflags "-s
          -w -X 'narelotl/buildinfo.InstallerGitHash=$(git rev-parse --short
          HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{ github.ref_name }}'" -o
          NarelotlCli-linux

      - name: Update executable
        run: |
          chmod +x Narelotl*

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Narelotl-linux
          path: |
            Narelotl-x11
            Narelotl-wayland
            Narelotl
            NarelotlCli-linux

  build-mac:
    runs-on: ${{ matrix.runner }}

    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: amd64
            runner: macos-15-intel
            artifact_name: Narelotl-macos
            gui_name: Narelotl-darwin-x64.zip
            cli_name: NarelotlCli-darwin-x64
          - arch: arm64
            runner: macos-latest
            artifact_name: Narelotl-macos-arm64
            gui_name: Narelotl-darwin-arm64.zip
            cli_name: NarelotlCli-darwin-arm64
    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.2"

      - uses: actions/cache@v5
        with:
          path: |
            ~/Library/Caches/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-${{ matrix.arch }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.arch }}-go-

      - name: Install dependencies
        run: brew install sdl2

      - name: Install Go dependencies
        run: go get -v

      - name: Build GUI
        run: CGO_CFLAGS="-mmacosx-version-min=10.15"
          CGO_LDFLAGS="-mmacosx-version-min=10.15" CGO_ENABLED=1 GOOS=darwin
          GOARCH=${{ matrix.arch }} go build -v -tags "static gui" -ldflags "-s
          -w -X 'narelotl/buildinfo.InstallerGitHash=$(git rev-parse --short
          HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{ github.ref_name }}'" -o
          Narelotl

      - name: Build CLI
        run: CGO_CFLAGS="-mmacosx-version-min=10.15"
          CGO_LDFLAGS="-mmacosx-version-min=10.15" CGO_ENABLED=1 GOOS=darwin
          GOARCH=${{ matrix.arch }} go build -v -tags "cli" -ldflags "-s
          -w -X 'narelotl/buildinfo.InstallerGitHash=$(git rev-parse --short
          HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{ github.ref_name }}'" -o
          ${{ matrix.cli_name }}

      - name: Update executable
        run: |
          chmod +x Narelotl*

      - name: Generate MacOS GUI bundle
        run: |
          mkdir -p Narelotl.app/Contents/MacOS
          mkdir -p Narelotl.app/Contents/Resources
          cp macos/Info.plist Narelotl.app/Contents/Info.plist
          mv Narelotl Narelotl.app/Contents/MacOS/Narelotl
          cp macos/icon.icns Narelotl.app/Contents/Resources/icon.icns

      - name: Import signing certificate
        env:
          CSC_LINK: ${{ secrets.APPLE_SIGNING_CERT }}
          CSC_KEY_PASSWORD: ${{ secrets.APPLE_SIGNING_CERT_PASSWORD }}
        run: |
          echo "$CSC_LINK" | base64 --decode > certificate.p12
          security create-keychain -p "" build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p "" build.keychain
          security import certificate.p12 -k build.keychain -P "$CSC_KEY_PASSWORD" -T /usr/bin/codesign
          security set-key-partition-list -S apple-tool:,apple:,codesign: -s -k "" build.keychain
          rm certificate.p12

      - name: Sign the executables
        run: |
          codesign --force --options runtime --sign "Developer ID Application" Narelotl.app/Contents/MacOS/Narelotl
          codesign --force --options runtime --sign "Developer ID Application" Narelotl.app
          codesign --force --options runtime --sign "Developer ID Application" ${{ matrix.cli_name }}

      - name: Notarize the executables
        env:
          API_KEY: ${{ secrets.APPLE_API_KEY }}
          APPLE_API_KEY_ID: ${{ secrets.APPLE_API_KEY_ID }}
          APPLE_API_ISSUER: ${{ secrets.APPLE_API_ISSUER }}
        run: |
          echo "$API_KEY" > apple.p8
          ditto -c -k --keepParent Narelotl.app Narelotl-notarize.zip
          ditto -c -k ${{ matrix.cli_name }} NarelotlCli-notarize.zip
          xcrun notarytool submit Narelotl-notarize.zip --key apple.p8 --key-id "$APPLE_API_KEY_ID" --issuer "$APPLE_API_ISSUER" --wait
          xcrun stapler staple Narelotl.app
          xcrun notarytool submit NarelotlCli-notarize.zip --key apple.p8 --key-id "$APPLE_API_KEY_ID" --issuer "$APPLE_API_ISSUER" --wait
          rm apple.p8 Narelotl-notarize.zip NarelotlCli-notarize.zip

      - name: Create final zip
        run: zip -r ${{ matrix.gui_name }} Narelotl.app

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.artifact_name }}
          path: |
            ${{ matrix.gui_name }}
            ${{ matrix.cli_name }}

  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Install Go
        uses: actions/setup-go@v6
        with:
          go-version: "1.26.2"

      - uses: actions/cache@v5
        with:
          path: |
            ~\AppData\Local\go-build
            ~\go\pkg\mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: Setup MSYS2
        uses: msys2/setup-msys2@v2
        with:
          path-type: inherit

      - name: Install Windows dependencies
        shell: msys2 {0}
        run: |
          pacman -S --noconfirm git mingw-w64-x86_64-gcc mingw-w64-x86_64-SDL2

      - name: Install Go dependencies
        shell: msys2 {0}
        run: |
          go get -v
          go install github.com/tc-hib/go-winres@latest

      - name: Build GUI
        shell: msys2 {0}
        run: |
          go-winres make --product-version "git-tag"
          CGO_ENABLED=1 GOOS=windows GOARCH=amd64 go build -v -tags "static gui" -ldflags "-s -w -H=windowsgui -extldflags=-static -X 'narelotl/buildinfo.InstallerGitHash=$(git rev-parse --short HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{ github.ref_name }}'" -o Narelotl.exe

      - name: Build CLI
        shell: msys2 {0}
        run: |
          CGO_ENABLED=1 GOOS=windows GOARCH=amd64 go build -v -tags "cli" -ldflags "-s -w -X 'narelotl/buildinfo.InstallerGitHash=$(git rev-parse --short HEAD)' -X 'narelotl/buildinfo.InstallerTag=${{ github.ref_name }}'" -o NarelotlCli.exe

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: Narelotl-windows
          path: |
            Narelotl.exe
            NarelotlCli.exe

  release:
    runs-on: ubuntu-latest
    needs: [build-linux, build-mac, build-windows]
    if: always() && !cancelled() && needs.build-linux.result == 'success' && needs.build-windows.result == 'success'

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: "Download Linux Artifacts"
        uses: actions/download-artifact@v8
        with:
          name: Narelotl-linux
          path: linux

      - name: "Download MacOS Artifacts"
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          name: Narelotl-macos
          path: macos

      - name: "Download MacOS ARM64 Artifacts"
        continue-on-error: true
        uses: actions/download-artifact@v8
        with:
          name: Narelotl-macos-arm64
          path: macos

      - name: "Download Windows Artifacts"
        uses: actions/download-artifact@v8
        with:
          name: Narelotl-windows
          path: windows

      - name: Delete existing release
        run: gh release delete "${{ github.ref_name }}" --yes || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create the release
        uses: softprops/action-gh-release@v3
        with:
          name: ${{ github.ref_name }}
          token: ${{ secrets.GITHUB_TOKEN }}
          generate_release_notes: true
          make_latest: ${{ !contains(github.ref_name, '-') }}
          prerelease: ${{ contains(github.ref_name, '-') }}
          draft: false
          fail_on_unmatched_files: false
          body: |
            This is release ${{ github.ref_name }} of Narelotl.
          files: |
            linux/Narelotl-x11
            linux/Narelotl-wayland
            linux/Narelotl
            linux/NarelotlCli-linux
            macos/Narelotl-darwin-x64.zip
            macos/Narelotl-darwin-arm64.zip
            macos/NarelotlCli-darwin-x64
            macos/NarelotlCli-darwin-arm64
            windows/Narelotl*.exe
